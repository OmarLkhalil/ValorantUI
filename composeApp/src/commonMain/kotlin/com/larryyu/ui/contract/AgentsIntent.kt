package com.larryyu.ui.contract

/*
====================================================
🧠 شرح تفصيلي — State و Intents في MVI Architecture
====================================================

🔹 أولاً: AgentsUIState (Data Class)
----------------------------------
✅ الغرض:
هي الحالة الحالية (UI State) الخاصة بالشاشة.
بنخزن فيها كل القيم اللي بتتحكم في شكل الواجهة حاليًا (الـ data اللي ظهرت، التحميل، الأخطاء، إلخ).

✅ ليه Data Class؟
لأننا بنتعامل مع حالة واحدة متغيرة بشكل مستمر (Mutable State).
مش محتاجين حالات منفصلة زي Loading / Error / Success.
لكن بنبدل القيم جوه نفس الكائن.

✅ المميزات:
- Immutable (ما بتتغيرش القيم الداخلية، بننشئ نسخة جديدة لما تتغير حاجة)
- سهلة النسخ (`copy()`)
- مناسبة جدًا لما يكون عندك أكثر من عنصر في الـ UI بتحدث باستمرار.

✅ أمتى أستخدم بدلها Sealed Class؟
لو كل حالة مختلفة تمامًا (زي Loading, Error, Success كل واحدة ليها شكل أو بيانات مختلفة تمامًا)،
يبقى ساعتها `sealed class` أنسب لأنها بتمثل حالات منفصلة مش مجرد قيم داخل نفس الحالة.

----------------------------------
🔹 ثانيًا: AgentsIntent (Sealed Interface)
----------------------------------
✅ الغرض:
هي الأفعال (Intents) أو الأحداث اللي المستخدم بيعملها أو الـ UI بيبعتها للـ ViewModel.
يعني كل Intent بيمثل نية أو حدث (زي "fetch data", "refresh", "open details").

✅ ليه Sealed Interface؟
- بتسهل إنك تفصل الـ Intents عن بعضها وتخلي كل ViewModel عنده Intents خاصة بيه.
- تقدر تستخدم `when(intent)` في ViewModel وتغطي كل الحالات بدون الحاجة لـ `else`.
- Sealed Interface بتسمح إن الكلاسات اللي بتنفذها تكون في ملفات مختلفة (مش لازم كلها في نفس الملف زي sealed class).

✅ الفرق بين Sealed Class و Sealed Interface:
- `sealed class` ممكن تحتوي على logic أو data (يعني تقدر تورث منها وتعمل دوال أو خصائص).
- `sealed interface` بتكون أخف (contract بس)، وغالبًا بنستخدمها للـ events/intents لأننا مش محتاجين shared logic.
- `sealed interface` تسمح إن الـ implementations تتوزع في أكتر من ملف،
  بينما `sealed class` لازم تكون كل الحالات في نفس الملف.

✅ المميزات:
- Type-safe: تقدر تتأكد إنك غطيت كل الأحداث المحتملة في الـ ViewModel.
- بتخلي الكود modular ومنظم جدًا.
- مناسبة جدًا في MVI Architecture لأنها بتعبر عن "Intents" المستخدم بوضوح.

----------------------------------
📘 مثال عملي:
----------------------------------
data class AgentsUIState(
    val agents: List<AgentsModel> = emptyList(),
    val isLoading: Boolean = false,
    val error: String? = null
)

sealed interface AgentsIntent {
    // لما المستخدم يفتح الصفحة لأول مرة
    data object FetchAgents : AgentsIntent

    // لما المستخدم يعمل سحب للتحديث (Pull-to-Refresh)
    data class RefreshAgents(val force: Boolean = false) : AgentsIntent

    // لما المستخدم يضغط على Agent علشان يفتح تفاصيله
    data class OpenDetails(val agentId: String) : AgentsIntent
}

----------------------------------
🧩 ملخص سريع:
----------------------------------
- 🟢 UiState → Data Class → تمثل الحالة الحالية للشاشة (loading, error, data)
- 🟣 Intents → Sealed Interface → تمثل الأفعال اللي بتحصل في الشاشة (fetch, refresh, click)
- 🟠 Sealed Class → لو عندك حالات UI مختلفة تمامًا (مثلاً: LoadingState, ErrorState, SuccessState)
*/
sealed interface AgentsIntent {
    data object FetchAgents : AgentsIntent
    data object RefreshAgents : AgentsIntent
    data class OpenDetails(val agentId: String) : AgentsIntent
}

sealed interface AgentDetailsIntent {
    data class FetchAgentDetails(val agentId: String) : AgentDetailsIntent
}